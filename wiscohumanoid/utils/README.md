*Reminder: **YOU** are the **CLIENT** and the **ROBOT** is the **HOST.***

First, run `python -m lerobot.robots.alohamini.lekiwi_host` to setup host environment inside the Docker container on the AlohaMini Jetson.